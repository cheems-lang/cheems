class Memory {
    static variables = {
        '$__PI__': Math.PI,
        '$__E__': Math.E,
        '$__DOGE__': '🐶'
    };

    static functions = {
        //primt: function() {console.log(...arguments)}
    };
}

export default Memory;