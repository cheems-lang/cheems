class Memory {
    static variables = {
        '$PI': Math.PI,
        '$E': Math.E
    };

    static functions = {
        //primt: function() {console.log(...arguments)}
    };
}

export default Memory;